"""Built-in themes.

Each ``.py`` file in this directory defines one theme by calling
``register_theme(Theme(...))``.  Themes are auto-discovered at import
time by ``theme.py`` — drop a new file here and it's available
immediately.

See ``docs/THEMES.md`` for the full guide on adding a new theme.
"""
